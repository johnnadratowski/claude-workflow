# TODO

Active backlog for this project. Used by every skill as "is this work in scope or already tracked?"

## Format

Each todo is a top-level `##` header with a one-paragraph description underneath. Add new ones at the bottom. Remove (or move to a CHANGELOG, if you keep one) when done — don't strikethrough.

If a todo is large enough to need sub-items, use a nested `###` per sub-item.

---

## Example todo: TODO replace with your own

A short description of what needs doing, why it matters, and what done looks like. If there's a related scenario in another doc, link to it.

If this is blocked on something, say so at the bottom: `**Blocked on:** <thing>`.
